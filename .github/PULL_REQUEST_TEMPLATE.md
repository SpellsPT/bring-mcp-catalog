## What this changes

<!-- What you observed, then what you changed. -->

## How it was tested

<!-- "Verified against my own list" is a fine answer. -->

- [ ] `npm test` passes (210 tests)
- [ ] `npm run build` succeeds

## If this touches matching or scoring

<!-- Delete this section if not applicable. -->

Before/after for at least a couple of real terms, verified against the **live** catalog rather
than a fixture — a thin test catalog does not reproduce most matcher behaviour:

| query | before | after |
| ----- | ------ | ----- |
|       |        |       |

- [ ] The write path (what gets attached to a list) is unchanged, or the change is deliberate
      and explained above
